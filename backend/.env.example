DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=ec_db

JWT_SECRET=your-secret-here
SENTRY_DSN=your-sentry-dsn-here

LOG_LEVEL=info

SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
# SMTP_USER=your-smtp-user     # 本番SMTP用（省略時は認証なし）
# SMTP_PASS=your-smtp-password # 本番SMTP用（省略時は認証なし）
MAIL_FROM="EC Shop <no-reply@example.com>"
